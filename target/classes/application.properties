# 服务器配置 - 关键：让外部可以访问
server.address=0.0.0.0
server.port=8080

# 应用名称
spring.application.name=blockchain-platform

# JSP视图配置
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# 静态资源配置
spring.web.resources.static-locations=classpath:/static/,file:${user.home}/blockchain-images/
spring.mvc.static-path-pattern=/static/**

# 文件上传配置
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# 日志配置
logging.level.com.book=INFO
logging.level.org.springframework=WARN
logging.level.org.apache.tomcat=WARN

# 编码配置
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# 如果您有数据库，取消注释并配置以下内容：
# spring.datasource.url=jdbc:mysql://localhost:3306/blockchain_db
# spring.datasource.username=your_username
# spring.datasource.password=your_password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jp